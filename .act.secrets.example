# Copy this file to .act.secrets (gitignored) and fill in real values.
# Pass to Act with:  act --secret-file .act.secrets

# GitHub token — for any workflows that call the GitHub REST API.
# A fine-grained PAT with read-only repo scope is sufficient for most cases.
GITHUB_TOKEN=
